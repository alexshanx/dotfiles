#!/bin/sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "Commit blocked: install gitleaks with 'brew install gitleaks'." >&2
  exit 1
fi

exec gitleaks git --config .gitleaks.toml --pre-commit --staged --redact .
